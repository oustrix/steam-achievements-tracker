## .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
publish/
*.user
*.suo

## Rider / Visual Studio / VS Code
.idea/
.vs/
.vscode/
*.userprefs

## Test results
[Tt]est[Rr]esult*/
coverage*.json
coverage*.xml
*.trx

## macOS
.DS_Store

## Local runtime data — never commit
*.db
*.db-shm
*.db-wal
secrets.json
appsettings.Local.json

## Fixtures must be anonymized before committing; raw captures stay local
testdata/**/*.raw.json

## Claude Code worktrees — local scratch, never committed
.claude/worktrees/

## Superpowers scratch — plans and reports written while working, not project
## documentation. A `git add -A` at merge time would otherwise commit it.
.superpowers/
